*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Build outputs — never tracked. APKs and profiles are produced, not authored.
build/
/app/release/
/app/nolock/
*.apk
*.aab
*.dm

# The release key. Losing it means every device must uninstall before it can
# update again, so it is kept outside the repository and backed up by hand.
*.jks
*.keystore
